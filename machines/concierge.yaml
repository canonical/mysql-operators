juju:
  model-defaults:
    logging-config: <root>=INFO; unit=DEBUG; juju.rpc=TRACE
providers:
  lxd:
    enable: true
    bootstrap: true
host:
  snaps:
    jhack:
      channel: latest/edge
      connections:
        - jhack:dot-local-share-juju snapd
